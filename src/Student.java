/**
 * Student class, which extends Person.
 * Shows an example of inheritance for JavaDocs.
 *
 * @author Alex
 * @version 1.0
 * @see Person
 */
public class Student extends Person {

    /**
     * Creates a Student object using the Person constructor.
     *
     * @param name the student's name
     */
    public Student(String name) {
        super(name);
    }
}
